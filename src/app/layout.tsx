export const metadata = {
  title: 'GetAutoReply - 24/7 WhatsApp Auto-Reply',
  description: 'Never miss a customer message. Simple WhatsApp auto-reply bot for Indian businesses.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
